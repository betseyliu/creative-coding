import React from 'react';
import Page from './Page';
import p5 from 'p5';

const blocks = [
  {
    name: 'demo',
    renderFunction: (p: p5) => {},
    height: 500,
    width: 500,
  },
];

export default () => <Page blocks={blocks} />;
